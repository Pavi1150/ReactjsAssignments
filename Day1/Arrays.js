let arr = [10, 20, "hello world", true, 30];
let a;
// index 0 , 1, 2,3

//adding
arr.push(40) // at the end
arr.unshift("welcome") //at the beggining
console.log(arr);

//to remove

arr.pop();

arr.shift();
console.log(arr);
